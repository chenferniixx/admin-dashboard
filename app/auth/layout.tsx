/**
 * Auth layout: minimal — login page controls its own full-height layout.
 */
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

# NextAuth (required for production)
NEXTAUTH_URL=http://localhost:3000
# Generate: openssl rand -base64 32  (or: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
NEXTAUTH_SECRET=

# Optional: override default demo admin credentials
# NEXTAUTH_DEMO_EMAIL=admin@example.com
# NEXTAUTH_DEMO_PASSWORD=admin123
